# Arquitectura de Schemas - Diagrama de Flujo

## 🏛️ Estructura General

```
┌─────────────────────────────────────────────────────────────┐
│                       src/                                   │
└──────────────┬───────────────────────────────┬──────────────┘
               │                               │
┌──────────────▼──────────────┐   ┌────────────▼───────────────┐
│     shared/schemas/         │   │      features/             │
│   (Schemas Reutilizables)   │   │     (Por Módulo)           │
└──────────────┬──────────────┘   └────────────┬───────────────┘
               │                               │
┌──────────────▼──────────────┐   ┌────────────▼───────────────┐
│   common/                   │   │   [modulo]/schemas/        │
│  - file.schema.ts           │   │  - order.schema.ts         │
│  - contact.schema.ts        │   │  - customer.schema.ts      │
│  - address.schema.ts        │   │  - ...                     │
└─────────────────────────────┘   └────────────────────────────┘
```

## 🔄 Flujo de Composición de Schemas

```
┌──────────────────────┐
│  FileUploadSchema    │ (src/shared/schemas/common/file.schema.ts)
│  (Reutilizable)      │
└──────────┬───────────┘
           │
           │ importado por
           ▼
┌──────────────────────┐
│    IssueSchema       │ (src/features/orders/schemas/issue.schema.ts)
│  ┌────────────────┐  │
│  │ issue_files:   │  │
│  │ FileUpload[]   │  │
│  └────────────────┘  │
└──────────┬───────────┘
           │
           │ usado en
           ▼
┌──────────────────────┐
│    OrderSchema       │ (src/features/orders/schemas/order.schema.ts)
│  ┌────────────────┐  │
│  │ issues:        │  │
│  │ IssueSchema[]  │  │
│  │                │  │
│  │ device_data:   │  │
│  │ DeviceSchema   │  │
│  │                │  │
│  │ customer_data: │  │
│  │ CustomerSchema │  │
│  └────────────────┘  │
└──────────┬───────────┘
           │
           │ usado en
           ▼
┌──────────────────────┐
│  NewOrderPage.tsx    │ (src/features/orders/pages/NewOrderPage.tsx)
│  ┌────────────────┐  │
│  │ useForm<       │  │
│  │  OrderFormData │  │
│  │ >({            │  │
│  │  resolver:     │  │
│  │   OrderSchema  │  │
│  │ })             │  │
│  └────────────────┘  │
└──────────────────────┘
```

## 📦 Flujo de Exportación/Importación

Obtención de schemas desde sus ubicaciones correctas:

```
┌─────────────────────────────────────────────────────────────┐
│             Schema Compartido (Shared)                       │
│  src/shared/schemas/common/contact.schema.ts                │
│                                                              │
│  export const ContactInfoSchema = z.object({...})           │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       │ exportado en src/shared/schemas/index.ts
                       ▼
┌─────────────────────────────────────────────────────────────┐
│             Schema de Feature (Modulo)                       │
│  src/features/orders/schemas/order.schema.ts                │
│                                                              │
│  import { ContactInfoSchema } from "@/shared/schemas"       │
│                                                              │
│  export const OrderSchema = z.object({                      │
│    contact: ContactInfoSchema                               │
│  })                                                         │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       │ exportado en src/features/orders/schemas/index.ts
                       ▼
┌─────────────────────────────────────────────────────────────┐
│                  Componente del Feature                      │
│  src/features/orders/pages/NewOrderPage.tsx                 │
│                                                              │
│  import { OrderSchema } from "../schemas"                   │
│  // O path absoluto:                                        │
│  import { OrderSchema } from "@/features/orders/schemas"    │
└─────────────────────────────────────────────────────────────┘
```

## 🎯 Patrón de Uso en Componentes

```
┌─────────────────────────────────────────────────────────────┐
│                    Componente React                          │
│                                                              │
│  1. Import                                                  │
│     ┌──────────────────────────────────────────┐           │
│     │ // Importar schema específico del modulo │           │
│     │ import { OrderSchema,                    │           │
│     │   type OrderFormData,                    │           │
│     │   orderDefaultValues                     │           │
│     │ } from "@/features/orders/schemas"       │           │
│     └──────────────────────────────────────────┘           │
│                                                              │
│  2. Setup Form                                              │
│     ┌──────────────────────────────────────────┐           │
│     │ const methods = useForm<OrderFormData>({ │           │
│     │   resolver: zodResolver(OrderSchema),    │           │
│     │   defaultValues: orderDefaultValues      │           │
│     │ })                                       │           │
│     └──────────────────────────────────────────┘           │
│                                                              │
│  3. Submit Handler                                          │
│     ┌──────────────────────────────────────────┐           │
│     │ const onSubmit = (data: OrderFormData) =>│           │
│     │   // data está validado y tipado         │           │
│     └──────────────────────────────────────────┘           │
│                                                              │
│  4. Render                                                  │
│     ┌──────────────────────────────────────────┐           │
│     │ <FormProvider {...methods}>              │           │
│     │   <form onSubmit={handleSubmit(onSubmit)}>│          │
│     │     {/* campos */}                       │           │
│     │   </form>                                │           │
│     │ </FormProvider>                          │           │
│     └──────────────────────────────────────────┘           │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## 🔑 Convenciones de Nombres

```
┌─────────────────────────────────────────────────────────────┐
│                    Elemento                                  │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Archivo:        [nombre].schema.ts                         │
│                  └─ device.schema.ts                        │
│                                                              │
│  Schema Zod:     [Nombre]Schema / [Nombre]DataSchema        │
│                  └─ DeviceDataSchema                        │
│                                                              │
│  Tipo TS:        [Nombre]FormData / [Nombre]Data            │
│                  └─ DeviceFormData                          │
│                                                              │
│  Defaults:       [nombre]DefaultValues                      │
│                  └─ deviceDefaultValues                     │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## 📋 Checklist de Creación

```
┌─────────────────────────────────────────────────────────────┐
│  Crear Nuevo Schema                                         │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ☐ 1. Determinar ubicación                                 │
│       ├─ ¿Es reutilizable por VARIOS modulos?              │
│       │   → src/shared/schemas/common/                     │
│       └─ ¿Es específico de UN modulo?                      │
│           → src/features/[modulo]/schemas/                 │
│                                                              │
│  ☐ 2. Crear archivo [nombre].schema.ts                     │
│                                                              │
│  ☐ 3. Definir Schema con JSDoc                             │
│       export const [Nombre]Schema = z.object({...})        │
│                                                              │
│  ☐ 4. Exportar tipo inferido                               │
│       export type [Nombre]FormData = z.infer<...>          │
│                                                              │
│  ☐ 5. Definir valores por defecto (si aplica)              │
│       export const [nombre]DefaultValues = {...}           │
│                                                              │
│  ☐ 6. Agregar al index.ts del directorio correspondiente   │
│       export * from "./[nombre].schema"                    │
│                                                              │
│  ☐ 7. Verificar mensajes en español                        │
│                                                              │
│  ☐ 8. Probar importación desde componente                  │
│       // Shared:                                           │
│       import { ... } from "@/shared/schemas"               │
│       // Feature:                                          │
│       import { ... } from "@/features/[modulo]/schemas"    │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## 🌳 Árbol de Dependencias (Ejemplo: OrderSchema)

```
OrderSchema (src/features/orders/schemas/order.schema.ts)
├── serviceType: number
├── description: string
├── device_data: DeviceDataSchema (Local Feature Schema)
│   ├── device_name: string
│   └── ...
├── issues: IssueSchema[] (Local Feature Schema)
│   ├── issue_name: string
│   └── issue_files: FileUploadSchema[] (Shared Schema)
│       ├── filename: string
│       └── ...
└── customer_data: CustomerDataSchema (Local Feature Schema)
    ├── customer_name: string
    └── preferred_contact: PreferredContactSchema (Shared Schema)
```
